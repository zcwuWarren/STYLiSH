package org.example.stylish;

import org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry;

public interface WebConfigInterface {
    void addViewControllers(ResourceHandlerRegistry registry);
}
